#include "main.h"
#include <stdio.h>

/**
 * add - adds two numbers togather
 * @i: The first number to be added.
 * @k: The second number to be added.
 * Return: Always 0.
 */

int add(int i, int k)
{
return (i + k);
} 
